// -----------------------------------------------------------------------------
// Offline transaction view
// Shows daemon-owned prepared updates and lets the user discard them.
// -----------------------------------------------------------------------------
#pragma once

struct MainWindowUiState;

void offline_transaction_view_show(MainWindowUiState *widgets);
