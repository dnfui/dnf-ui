# Fedora packages needed for native DNF UI development.
# This file is read by the installer script in utils.
# Keep this list in sync with dnf-ui.spec and the Dockerfile.
#
# The list covers native builds, unit tests, package validation, RPM builds,
# translations, and dnf5daemon based app testing.

appstream
catch-devel
dbus-daemon
desktop-file-utils
gcc-c++
gettext
git
glib2-devel
gtk4-devel
libdnf5-devel
make
meson
mock
ninja-build
pkgconf
polkit
rpmlint
rpm-build
